[pytest]
markers =
    integration: marks tests that require external services
    slow: marks tests that are slow to execute
testpaths =
    tests
python_files =
    test_*.py
    *_test.py
addopts =
    -v
    --strict-markers